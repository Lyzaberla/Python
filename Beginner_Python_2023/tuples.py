#a tuple is a type of data structure like lists but has some 
#differenes... tuples comes in brackets
#tuples are immutable...elements cannot be changed
#uses the same indexing format
coordinates = (4,5)
print(coordinates[1])

#to create a list of tuples
coordinates = [(1,2), (3,4), (5,6)]
print(coordinates)
print(coordinates[2])
